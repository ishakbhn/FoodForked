<p id="notice"><%= notice %></p>

<h1>Plans</h1>

<%= link_to 'New Plan', new_plan_path %>
<table>
  <thead>
    <tr>
      <th><%= sortable "date" %></th>
      <th><%= sortable "breakfast" %></th>
      <th><%= sortable "lunch" %></th>
      <th><%= sortable "dinner" %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @plans.each do |plan| %>

    <% lunch = plan.lunch_id.to_i - 1 %>
    <% dinner = plan.dinner_id.to_i - 1 %>

      <tr>
          <td><%=  link_to plan.date, "/plans/#{plan.id}" %></td>
          <td><%= plan.food.name %></td>
          <td><%= @foods[lunch].name %></td>
          <td><%= @foods[dinner].name  %></td>
        </a>
      </tr>

      <tr>

    </tr>



    <% end %>
  </tbody>
</table>

<br>

